Security weekly scan workflow (patched)

This branch adds installation of jq and uses GITHUB_TOKEN + REST API to create issues from the runner instead of relying on gh CLI being available.

Branch: ci/fix-security-weekly-20260727
